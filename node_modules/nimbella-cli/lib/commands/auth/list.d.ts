import { NimBaseCommand, NimLogger } from 'nimbella-deployer';
import { CredentialRow } from 'nimbella-deployer';
export default class AuthList extends NimBaseCommand {
    static description: string;
    static flags: typeof NimBaseCommand.flags;
    static args: any[];
    runCommand(rawArgv: string[], argv: string[], args: any, flags: any, logger: NimLogger): Promise<void>;
    formatCredentialList(credentialList: CredentialRow[], logger: NimLogger): Promise<void>;
}
