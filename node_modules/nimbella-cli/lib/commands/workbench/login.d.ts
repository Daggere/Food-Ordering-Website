import * as WorkbenchRun from './run';
import { NimBaseCommand, NimLogger } from 'nimbella-deployer';
export default class WorkbenchLogin extends NimBaseCommand {
    static description: string;
    static flags: typeof WorkbenchRun.default.flags;
    static args: any[];
    static aliases: string[];
    runCommand(rawArgv: string[], argv: string[], args: any, flags: any, logger: NimLogger): Promise<void>;
}
