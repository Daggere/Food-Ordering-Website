import { DeployStructure, Includer, ProjectReader, Feedback } from './deploy-struct';
interface TopLevel {
    web: string;
    packages: string;
    config?: string;
    env?: string;
    strays: string[];
    filePath: string;
    githubPath: string;
    includer: Includer;
    reader: ProjectReader;
    feedback: Feedback;
}
export declare function readTopLevel(filePath: string, env: string, includer: Includer, mustBeLocal: boolean, feedback: Feedback): Promise<TopLevel>;
export declare function buildStructureParts(topLevel: TopLevel): Promise<DeployStructure[]>;
export declare function assembleInitialStructure(parts: DeployStructure[]): DeployStructure;
export {};
