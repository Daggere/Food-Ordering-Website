import { Bucket } from '@google-cloud/storage';
import { Credentials, WebResource, DeployResponse, BucketSpec, VersionEntry, ProjectReader, OWOptions } from './deploy-struct';
export declare function openBucketClient(credentials: Credentials, bucketSpec: BucketSpec): Promise<Bucket>;
export declare function deployToBucket(resource: WebResource, client: Bucket, spec: BucketSpec, versions: VersionEntry, reader: ProjectReader, owOptions: OWOptions): Promise<DeployResponse>;
export declare function computeBucketStorageName(apiHost: string, namespace: string): string;
export declare function computeBucketDomainName(apiHost: string, namespace: string): string;
export declare function cleanBucket(client: Bucket, spec: BucketSpec, owOptions: OWOptions): Promise<string>;
export declare function restore404Page(client: Bucket, owOptions: OWOptions): Promise<string>;
