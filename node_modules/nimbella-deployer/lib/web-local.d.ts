import { WebResource, BucketSpec, DeployResponse } from './deploy-struct';
export declare function ensureWebLocal(webLocal: string): string;
export declare function deployToWebLocal(resource: WebResource, webLocal: string, spec: BucketSpec): Promise<DeployResponse>;
