import { Credentials, FullCredentials } from './deploy-struct';
import { Persister } from './credentials';
export declare function doLogin(token: string, persister: Persister, host?: string): Promise<Credentials>;
export declare function doAdminLogin(apihost: string): Promise<Credentials>;
export declare function doInteractiveLogin(newCreds: FullCredentials, persister: Persister): Promise<Credentials>;
